import { Image } from 'mdx-deck';
import {
  FullScreenCode,
  Main,
  Section,
  SectionInverted,
} from 'gatsby-mdx-theme';

import { FullScreenImage } from '../layouts';
import { Link } from '../components';

export default SectionInverted;

# Search Engine Optimization

## Hereafter SEO because... optimization.

---

## You want this

---

## It's easy with Gatsby

---

## `react-helmet`

### A library to render `meta` tags

---

## Gatsby uses SSR (at build time) to generate static content

---

## `meta` tags included

### no JavaScript required!

---

export default FullScreenImage({
  src: `https://media.giphy.com/media/EhpBNBiCaRmLe/giphy.gif`,
});

## There are lots of... _layers_ here

---

## Gatsby provides you tooling to implement cleanly

### `react-helmet`, static rendering, and GraphQL data layer = ❤️

---

## We also provide an SEO component

### Sane, extensible defaults

---

export default FullScreenCode;

```jsx
import React from 'react';

import SEO from '../components/seo';

export default function Template() {
  return (
    <div>
      <SEO
        title="My great blog post"
        description="This damn well better be less than 160 characters"
      />
    </div>
  );
}
```

---

## Once-more, use GraphQL data layer to our advantage

---

## We can query for excerpts, titles, and... images!

### and stitch this data together!

---

## Social sharing cards ez pz

---

export default FullScreenCode;

```graphql
{
  file(relativePath: { regex: "/some-image.jpg/" }) {
    childImageSharp {
      resize(width: 1200) {
        src
        height
        width
      }
    }
  }
}
```

---

<img src="assets/social-sharing.png" style={{ maxHeight: '70vh' }} />

---

## Extra extra!

# [Read all about it!](https://blog.dustinschau.com/search-engine-optimization-with-gatsby)

---

export default FullScreenImage({
  darken: 0.25,
  src: `https://media.giphy.com/media/6s7mSIPWYALTDOBwID/giphy.gif`,
});

## Lab 04

### Implementing SEO

<Link href="/labs/04-implementing-seo/">Take me to the Lab</Link>
