import {
  FullScreenCode,
  Main,
  Section,
  SectionInverted,
} from 'gatsby-mdx-theme';
import { LiveCode } from 'mdx-deck-live-code';
import { FullScreenImage } from '../layouts';
import { Link } from '../components';

export default SectionInverted;

## Gatsby

### A modern app compiler for React

---

- 30,000 GitHub stars
- 1500 contributors (growing every day)
- Yes - a company.

---

## What's an app compiler?

### Internalizes best practices and makes them available for free, merely by using!

---

## What's static code generation?

### JSX _eventually_ produces HTML. Gatsby short-circuits this for performance!

---

### HTML, the way we used to write it!

---

export default FullScreenCode;

```jsx
<div id="___gatsby">
  <header>
    <h1>Hello World</h1>
  </header>
  <main>
    <p>This is some content</p>
  </main>
</div>
```

---

## Your app will load without Javascript... 🔥 (quickly!)

---

## Extremely modular architecture

### Plugins to do _anything!_

---

- `gatsby-plugin-react-helmet` (SEO)
- `gatsby-source-filesystem` (query your local file system)
- `gatsby-plugin-offline` (offline Progressive Web App)
- [and a _lot_ more!](https://www.gatsbyjs.org/plugins/)

---

## Bring your own data layer

### A core idea of Gatsby is that it's data agnostic

---

## Source(s)

- Local Markdown files
- Remote GraphQL API
- Wordpress (😱)
- and... Drupal!

---

# GraphQL

---

## A _query language_ for your API

---

export default FullScreenCode;

```graphql
query {
  user {
    name
    manager {
      name
    }
  }
}
```

---

export default FullScreenCode;

```json
{
  "data": {
    "user": {
      "name": "Dustin Schau",
      "manager": {
        "name": "N/A"
      }
    }
  }
}
```

---

## GraphiQL

<div>
  <iframe
    src="https://711808k40x.sse.codesandbox.io/___graphql?query=%7B%0A%20%20site%20%7B%0A%20%20%20%20siteMetadata%20%7B%0A%20%20%20%20%20%20title%0A%20%20%20%20%7D%0A%20%20%7D%0A%7D%0A"
    style={{ height: '80vh', width: '80vw' }}
  />
</div>

---

## Gatsby leverages GraphQL at _Build Time_ 🎉

---

## Gatsby parses queries in React components

- Use (local) schema to query against
- Parse queries, and inject components with data
- No run-time cost _at all_ for this
- e.g. you don't hit the CMS client-side

---

## Let's take a look

---

export default FullScreenCode;

```jsx
import React from 'react';
import { graphql } from 'gatsby';

export default function IndexPage({ data }) {
  return <h1>{data.site.siteMetadata.title}</h1>;
}

export const indexQuery = graphql`
  {
    site {
      siteMetadata {
        title
      }
    }
  }
`;
```

---

- The shape _matches_ the data
- The query is compiled away (no run time cost!)
- Static HTML is produced (🎉)

---

export default FullScreenCode;

```html
<div id="___gatsby">
  <h1>Your App Name</h1>
</div>
```

---

## Gatsby is... amazing.

---

## Gatsby is _your_ web compiler.

---

## Note

### Gatsby is non-opinionated about style/css

### We support CSS Modules, CSS in JS, plain CSS, etc.

---

## The labs don't require style, but if you want to...

### Try [CSS Modules](https://www.gatsbyjs.org/docs/css-modules/)

---

export default FullScreenCode;

```css
.item {
  color: blue;
}

.other {
  color: red;
}
```

---

export default FullScreenCode;

```jsx
import React from 'react';

import styles from './whatever.module.css';

export default function Hello() {
  return (
    <div>
      <h1 className={styles.item}>I am blue</h1>
      <h1 className={styles.other}>I am red</h1>
    </div>
  );
}
```

---

![What we're building](https://gatsby-drupal-blog.netlify.com/)

---

export default FullScreenImage({
  darken: 0.5,
  src: `https://media.giphy.com/media/11LKx7277LkE0M/giphy.gif`,
});

## Lab 01

### Getting Started and Set Up

<Link href="/labs/01-set-up/">Take me to the Lab</Link>
